# DroneBridge — руководство пилота

## Схема связи

```
Дрон (ESP32) → 📱 хотспот телефона → 4G → ☁️ VPS (Oracle) → 🌐 → 💻 Mission Planner
```

Телефон — только точка доступа. Белый IP не нужен — работает через VPS-реле.

## Перед полётом

### 1. Телефон
- Включить хотспот **LEO** (пароль `88888888`).
- Полоса **2.4 ГГц**, безопасность **WPA2** (WPA3-Only НЕ поддерживается ESP32).
- Мобильные данные **ВКЛ**.
- Телефон рядом с дроном (сигнал WiFi).

### 2. Питание ESP32
Подать питание → ESP32 сам подключится к LEO и начнёт слать телеметрию на VPS.
Проверка: STATUS в терминале → `WiFi: OK` и `IP` не `0.0.0.0`.

### 3. Подключение Mission Planner
1. Кнопка **Connect**.
2. **TCP** → Host: `152.70.51.224` **Port:** `14552` (надёжно через 4G, TCP-мост на VPS)
   или **UDPCl** → Host: `152.70.51.224` **Port:** `14551`.
3. Нажать OK. Через 3–5 с зелёная полоса — связь есть.

## Полёт

- Арм/взлёт/миссию управляет FC по своей логике (ARM → AUTO → миссия).
- Калибровка компаса запускается автоматически только в окно 20 с после MAG_OK (синее мигание): наклоните дрон >45°.
- После калибровки — **перезагрузить FC** (отключить питание).

## Реле (сброс груза/парашют)

Срабатывает автоматически при **завершении миссии после реального взлёта** (>1 м) И при **краше**:

| Краш | Признак |
|---|---|
| Net | завис в препятствии (высота стабильна, скорость 0, газ >45 — 3 с) |
| Tumble | резкое вращение/кувырок (гиро >274°/с — 150 мс) |
| RapidDescent | падение >5 м/с вниз (500 мс) |

Если дрон упал, но снова полетел — это **не краш**, реле не сработает.
Ручное включение: команда `RELAY` в терминале.

## Если не работает

| Симптом | Причина | Решение |
|---|---|---|
| Нет зелёной полосы | Не нажат Connect | Нажми Connect (TCP 14552) |
| Нет зелёной полосы | Телефон без 4G | Включи мобильные данные |
| WiFi: NO | Хотспот выключен/5ГГц/WPA3 | Включи LEO на 2.4 ГГц WPA2 |
| WiFi: NO | Дрон далеко от телефона | Поднеси телефон ближе |
| No Heartbeat | Дрон без питания | Подай питание, жди 30 с |
| Реле сработало ложно | Battery failsafe на земле | Проверь BATT_MONITOR/VOLT_PIN в MP |
| Параметры грузятся долго | 4G слабый | Подожди, связь сама доедет |

## Терминал ESP32 (USB, COM14, 115200)

| Команда | Действие |
|---|---|
| `STATUS` | Статус: WiFi, IP, температура, CPU, RSSI |
| `SSID=...` / `PASS=...` | Настроить WiFi |
| `SAVE` | Сохранить и перезагрузить |
| `RELAY` | Включить реле вручную |
| `DISARM` | Принудительный disarm |
