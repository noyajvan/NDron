# DroneBridge — Руководство пилота

## Схема связи

```
Дрон (ESP32) → 📱 Хотспот телефона → 4G → ☁️ VPS (DO) → 🌐 Интернет → 💻 Ноутбук

Телефон — только точка доступа. Ни Tailscale, ни VPN не нужны.
```

## Что нужно

- ESP32-S3 (встроен в дрон) с прошивкой DropCtrlV3
- Телефон с WiFi хотспотом и мобильным интернетом (4G/5G)
- Ноутбук с Mission Planner и интернетом
- VPS (уже настроен: 134.209.206.127, Amsterdam)

## Перед полётом

### 1. Включить хотспот на телефоне

- SSID: `LEO`
- Пароль: `88888888`
- Мобильные данные: **ВКЛ** (без них ESP32 не выйдет в интернет)

### 2. Подать питание на ESP32

ESP32 сам подключится к LEO и начнёт отправлять телеметрию на VPS.

Проверить: светодиод ESP32 горит синим.

### 3. На ноутбуке

1. Открыть Mission Planner
2. Кнопка **Connect** ⬆ (правый верхний угол)
3. Выбрать **UDPCl** (уже сохранён)
4. **Host:** `134.209.206.127` **Port:** `14551` (уже заполнены)
5. Нажать **OK**

Через 3-5 секунд зелёная полоса — связь есть!

Настройки запоминаются — в следующий раз достаточно просто нажать Connect.

### 4. Переключение дронов

Если летать по очереди (один за другим):

1. Выключить дрон 1
2. Включить дрон 2
3. Через 5 секунд телеметрия дрона 2 пойдёт в MP автоматически
4. SYS_ID в MP покажет какой дрон подключён

Если нужно изменить номер дрона (SYS_ID):

```
Подключи ESP32 к USB
Открой монитор порта (115200 бод)
Введи: SYSID=2
Введи: SAVE
```

## Если не работает

| Симптом | Причина | Решение |
|---------|---------|---------|
| Нет зелёной полосы | Не нажат Connect | Нажми Connect — настройки уже вбиты |
| Нет зелёной полосы | Телефон без 4G | Включи мобильные данные |
| Нет зелёной полосы | ESP32 без питания | Подай питание, жди синего LED |
| "No Heartbeat" | Дрон не в воздухе | Проверь подключение ESP32 к хотспоту |
| Связь прерывается | 4G/хотспот нестабилен | Перезагрузи телефон |

## VPS параметры

- Сервер: `134.209.206.127` (DigitalOcean Amsterdam, 45ms ping)
- Порт дрона: **14550** (телеметрия от ESP32)
- Порт GCS: **14551** (команды от MP)
- Relay: systemd-сервис `udp-relay` (запущен всегда, автозапуск)

## Файлы (для справки)

| Файл | Назначение |
|------|-----------|
| `src/main_DrnBrdg.cpp` | Прошивка ESP32 |
| `scripts/connect.ps1` | Запуск MP с автонажатием Connect |
| `connect.bat` | Быстрый запуск connect.ps1 |
